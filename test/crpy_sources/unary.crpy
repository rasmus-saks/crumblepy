# Test unary operators: 'not' and unary '-'
# Expected outcome:
#   When C goes below (or is equal to) 100, Sparkle 0 is set to green
#   When C goes above 100, Sparkle 1 is set to red
while True:
    if not (get_analog(C) > 100):
        set_sparkle(0, 0, -20 * -1, 0)
    else:
        set_sparkle(0, 20, 0, 0)